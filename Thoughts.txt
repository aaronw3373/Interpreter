-	Need a main "conductor" function that goes through the parse tree and calls appropriate 
	helpers, this will return the final return value, and it will be called in the initial 		
	"interpret" function, taking parser (filename) as its input parameter.